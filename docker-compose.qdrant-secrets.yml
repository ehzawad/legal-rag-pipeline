services:
  api:
    environment:
      QDRANT_API_KEY: ""
      QDRANT_API_KEY_FILE: /run/secrets/qdrant_api_key
    secrets:
      - qdrant_api_key

secrets:
  qdrant_api_key:
    file: ${QDRANT_API_KEY_FILE:-./secrets/qdrant_api_key}
